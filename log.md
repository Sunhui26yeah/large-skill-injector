# Log

Append-only. History is never rewritten.

- [2026-08-13 23:30] init: Initialized this repo following Karpathy's LLM Wiki pattern (CLAUDE.md / index.md / log.md), set it up as its own git repo, planned to push to the GitHub private repo `large-skill-injector`. Also configured a cloud routine to run daily at 23:59 PDT, pulling that day's news for the top 10 companies by global market cap into `daily/YYYY-MM-DD_top10_corp.md`.
- [2026-08-13 23:50] ingest: Cloud routine creation got stuck on "GitHub account not connected"; manually tested the fetch flow once in the current session instead, generating `daily/2026-08-13_top10_corp.md` and registering it in index.md. This was to validate the prompt logic, not an automated output.
- [2026-08-14 00:05] ingest: Per user request, autonomously explored GitHub for LLM-wiki / second-brain repo patterns (Karpathy's original gist, NicholasSpisak/second-brain, Dendron/Logseq/Foam, Andy Matuschak's evergreen notes), written up as `concepts/LLM_Wiki_Design_Patterns.md`; also split the CRA FAQ notes already organized in Misc/ into 8 cross-linked pages under `concepts/CRA/` (overview + 7 chapters, 76 Q&A items). Updated CLAUDE.md's directory conventions accordingly to the concepts/entities/synthesis subdirectory split + relative-path link convention (not Obsidian wikilink syntax). index.md registered with the new pages.  This was the first iteration of an overnight self-paced autonomous exploration session (/loop dynamic mode, targeting a stop time of 2026-08-14 07:00 PDT).
- [2026-08-14 00:45] lint/ingest: `git push` got stuck on an expired credential in the local keychain ("Invalid username or token" → retrying produced "could not read Username: Device not configured"); committed but not yet pushed, pending the user resolving it. While local edits were still possible, converted 24 "see also X.Y" plain-text cross-references already present in the CRA FAQ source (but not yet turned into links) into real relative-path anchor links via script (21 resolved and replaced successfully; 3 pointed to material outside this repo — preamble sections of the Cyber Resilience Act itself, the "Blue Guide," etc. — and were left as plain text). Anchors were generated using the standard GitHub heading-slug algorithm (lowercase, strip punctuation, spaces to hyphens); this works with most markdown renderers but isn't guaranteed to match GitHub's algorithm 100% of the time.
- [2026-08-14 01:15] ingest: Enabled the `entities/` subdirectory — created a one-page profile (one-line business description + "related activity" section) for each of the 10 companies that appeared in that day's top-10 digest, linked forward from each company heading in `daily/2026-08-13_top10_corp.md`, with each entity page linking back to that day's anchor, forming bidirectional cross-references. Anchors were computed precisely via script (stripping markdown link syntax before computing the slug) to avoid manual guessing errors — a few typos did slip in by hand along the way (extra hyphens, "TSMC" mistyped as "tm"), fixed once the script-computed values were applied uniformly. Ran a repo-wide link lint afterward (script scanning every `](...)` target path for existence); no broken links found aside from placeholder example paths in CLAUDE.md/the design-patterns page (not real links). index.md registered with all 10 entity pages.
- [2026-08-14 22:41] ingest: Enabled the `Academic_skills/` subdirectory (a new category, how-to pages, distinct from the fact/concept-oriented `concepts/`), writing its first page, `Markdown_to_PDF_Without_Toolchain.md` — a writeup of a just-completed task: converting `ESEM2026Replication/REVIEW_TODO.md` to PDF with no pandoc/wkhtmltopdf/weasyprint/LaTeX toolchain available locally, using a python-markdown + headless-Chrome-print combination. Covers GFM checkbox preprocessing, a gotcha where emoji used as checkbox glyphs collapse to the same box shape under the PDF renderer, and a technique for screenshot-verifying multi-page renders with headless Chrome. CLAUDE.md's directory conventions and index.md registered the new category.
- [2026-08-14 23:05] lint: User corrected that this entire wiki (schema, index, log, and every content page) must be written in English — the repo had drifted into Chinese since the initial pages were written that way and later pages matched the existing convention. Per user's explicit choice to translate everything (not just new pages going forward), retranslated CLAUDE.md, index.md, log.md (this file — earlier entries translated in place, not rewritten in substance), the Academic_skills page, `concepts/LLM_Wiki_Design_Patterns.md`, the daily digest page (already English, no change needed), and all 10 entity pages. Renamed every Chinese-named file to an English filename (`git mv`, preserving history). Mid-translation, user decided the `concepts/CRA/` pages (EU Cyber Resilience Act FAQ notes, 8 files, 76 Q&A items) weren't currently useful and asked to delete them outright rather than translate them — removed via `git rm -r`, deregistered from `index.md`, and scrubbed the now-dangling `concepts/CRA/` example references from `CLAUDE.md` and the Academic_skills page (the `LLM_Wiki_Design_Patterns.md` page's own CRA-specific example paragraph was rewritten to be generic rather than deleted outright, since the underlying design point still holds). Also fixed a pre-existing dangling cross-reference in `entities/NVIDIA.md` that pointed to a "dynamic rankings" explanation in the design-patterns page that never actually existed there. Fixed all cross-links repo-wide. Saved a feedback memory (English-only convention for this repo) so future sessions don't need this correction again.
- [2026-08-14 23:10] lint: User asked to rename `Academic_skills/` to `academic_skill/` (lowercase, singular). Renamed via `git mv` (preserves history) and updated the current-state references in `index.md`, `CLAUDE.md`, and the page's own self-reference; left the directory name as-is in the two prior log entries above, since it genuinely was called `Academic_skills/` at the time those entries were written — append-only history isn't rewritten just because something later got renamed.
- [2026-08-14 23:02] lint/ingest: User asked whether the daily top-10-corp cloud routine (documented in CLAUDE.md but never confirmed working, per the 2026-08-13 log entries) was actually running. Checked via the RemoteTrigger API and found a routine (`large-skill-injector-daily-top10-digest`, id `trig_01XqPqdgDPGKMUDrBrXzM6mX`) already existed, created 2026-08-14T15:35:43Z, enabled, correctly scheduled (`59 6 * * *` UTC = 23:59 PDT) — so the "GitHub account not connected" blocker from the earlier session must have been resolved without ever getting logged here. However, its prompt was missing pieces of the established `daily/2026-08-13_top10_corp.md` format: entity-page cross-links in each heading, the market-cap value in the heading, and the closing italic market-context paragraph. Updated the routine's prompt via `RemoteTrigger update` to require all three, and triggered a manual test run (`RemoteTrigger run`) to confirm the GitHub push actually succeeds this time, rather than waiting for the next scheduled fire.
- [2026-08-14 23:13] ingest: Added a second parallel automated daily source, per user request: a people-watch digest. Created `daily/people_watchlist.md` with the user-supplied list of 20 tracked individuals (AI labs/model developers, chips/infrastructure, big tech CEOs, AI safety/academia, capital/governance), each with role context. Documented the new source in CLAUDE.md (new "On the daily people-watch digest" section, parallel to the company-digest one) and registered it in index.md under a new "Daily People-Watch Digest" category. Deliberately did NOT create `entities/` profile pages for the 20 people — out of scope unless requested, avoids building a layer speculatively. Created the actual cloud routine (`large-skill-injector-daily-people-watch`) via `RemoteTrigger create`, scheduled for 23:45 PDT (`45 6 * * *` UTC, 15 minutes before the company digest so the two don't hit GitHub in the same minute). The routine reads `daily/people_watchlist.md` fresh on every run rather than having the name list hardcoded in its prompt, so future watchlist edits take effect automatically without touching the routine itself. Per user's explicit acknowledgment, the routine has no authenticated LinkedIn/X API access — it does best-effort web search (including site-scoped queries) and may under-detect LinkedIn-specific posts.
- [2026-08-16 22:45] lint: User asked why the daily digests hadn't been showing up. Despite the 2026-08-14 23:02 entry claiming the push blocker was resolved, both cloud routines had actually never successfully pushed since creation — confirmed by fetching `origin/main` directly and finding no commits from either routine's fires on 2026-08-14/15, and by checking `gh api repos/Sunhui26yeah/large-skill-injector/collaborators`, which showed only the human owner as a collaborator (no GitHub App/bot identity had write access). Also noted in passing: this repo is actually public (`private: false` per the GitHub API), not private as CLAUDE.md/SKILL.md describe — clone-without-auth was masking the missing write credential. User fixed the GitHub App/environment permissions on their end; re-ran the dormant one-off `large-skill-injector-push-connectivity-test` trigger (`trig_01XRLCRXxv9FRMQPennrm7CG`) manually via `RemoteTrigger run` to confirm rather than waiting for the next scheduled digest — push succeeded this time (commit `cefdf7d`). Removed the resulting `PUSH_TEST.md` afterward. The two daily routines should now actually work going forward; still missing digest pages for 2026-08-14 and 2026-08-15 (company) and 2026-08-14/15 (people-watch), not backfilled here since the user only asked to confirm the fix, not to backfill.
- [2026-08-16 23:10] ingest: User asked to register two sources as the primary places to check whenever asked about 2027 new-grad job openings going forward: `https://haitou.zhitongguigu.com/newJob` (留学生海投网, international-student-focused board — fetched live to describe its actual scope: roles/geos/login requirements, rather than guessing from the URL alone) and `https://github.com/speedyapply/2027-AI-College-Jobs/blob/main/NEW_GRAD_USA.md` (community-maintained AI/ML new-grad US listing, part of the `speedyapply` repo family discussed earlier in this session alongside `vanshb03/New-Grad-2027`, `zapplyjobs/New-Grad-Jobs-2027`, and their associated Discord servers). Created `concepts/New_Grad_2027_Job_Search_Sources.md` describing what each source covers and how to cross-reference them (GitHub list = AI/ML + US-only + authoritative for that slice; haitou = broader role/geo coverage + international-student/visa angle, noisier). Registered in index.md under Concepts.
- [2026-08-19 14:50] ingest: User asked for the two nightly digest files' links to be emailed to hsun26@ncsu.edu (subject `[Daily Digest]`), and for the mechanism to automatically cover any future daily routine too, not just the current two. Rather than adding an email step to each of the two existing routines individually (which would need to be repeated for every future source), created a third standing cloud routine (`large-skill-injector-daily-digest-email`, id `trig_01GoH9JgwMEqNZaq9Qs5Q4fG`) scheduled at 00:20 America/Los_Angeles (`20 7 * * *` UTC), after both existing digests. It reads `git log --since="4 hours ago" --diff-filter=A --name-only -- daily/` to find whatever files were newly pushed that night — regardless of which routine pushed them — and sends one consolidated `[Daily Digest]` email with a link to each. Sends nothing if the window is empty. Documented the contract in CLAUDE.md (new "On the daily digest email" section): any future daily routine is picked up automatically as long as it writes a new file under `daily/`, commits/pushes it to `main`, and finishes within the routine's ~20:20–00:20 PT lookback window — no changes to the digest-email routine itself are needed.
- [2026-08-19 15:05] ingest: User asked for the two 2027 new-grad job search sources already registered in `concepts/New_Grad_2027_Job_Search_Sources.md` (留学生海投网 and `speedyapply/2027-AI-College-Jobs`) to also show up in the daily email, just as static links. Updated `large-skill-injector-daily-digest-email` via `RemoteTrigger update`: added a fixed "Job Search Sources" section with both URLs, always included regardless of whether any `daily/` files were found that night, and changed the routine to always send the email now (previously it skipped sending on an empty night — no longer applicable since the static section always has content). Updated CLAUDE.md's "On the daily digest email" section to describe the always-send behavior and the static links, and to note they should be kept in sync with the concepts page if either changes.
- [2026-08-20 00:00] ingest: User asked to save the US visa scheduling website (`https://www.usvisascheduling.com/zh-CN/`) so it can be returned quickly next time asked about the US visa website. Created `concepts/US_Visa_Scheduling_Website.md` and registered it in index.md under Concepts.
- [2026-08-24 00:00] ingest: User asked to save the PerfAgent paper (arXiv 2607.19653, MIT/RPI/IBM/MSU, Jul 2026) after reading its abstract together, plus the comparison discussed against Hui's candidate PhD thesis idea (MCTS-pruned search over a self-evolving LLM-curated knowledge base for PyTorch performance optimization, from `project-thesis-directions-2026` in Claude's auto-memory, 2026-08-19 entry). Created `concepts/PerfAgent_2607_19653.md` (paper summary: profiler-guided verifier-in-the-loop agent workflow for repo-level code optimization, three identified agent failure modes, results on GSO/SWE-efficiency-Lite) and `synthesis/PerfAgent_vs_MCTS_KB_PyTorch_Perf_Idea.md` (comparison: PerfAgent broader on target domain, Hui's idea broader on search mechanism — explicit MCTS branching vs. single-chain retry — and on knowledge use — self-evolving structured KB vs. relying on the base agent's static knowledge). Flagged PerfAgent as the closest existing related-work baseline for that thesis direction. Registered both pages in index.md under Concepts and Synthesis respectively.
- [2026-08-28 00:00] ingest: User asked to save the NC State CSC PhD graduate seminar check-in page (`https://webapps.csc.ncsu.edu/seminar_checkin/index.php`) with keyword "csc phd graduate seminar requirements". WebFetch confirmed the page is gated behind NC State's Microsoft/Shibboleth SSO (redirects to `login.microsoftonline.com` for Unity ID auth), so page contents (specific attendance requirement numbers, deadlines) couldn't be captured directly — left as an open item for a future ingest once seen while logged in. Created `concepts/NC_State_CSC_PhD_Seminar_Checkin.md` and registered it in index.md under Concepts.
- [2026-09-02 21:30] ingest: User asked to add Fei-Fei Li and her company World Labs to the daily digest. Added `Fei-Fei Li — Co-founder & CEO, World Labs; Co-director, Stanford HAI` to the "AI Labs / Model Developers" group in `daily/people_watchlist.md`. World Labs is private and nowhere near the top 10 by market cap, so it cannot go in the company digest (which is strictly top-10-by-market-cap and computed fresh each run); instead added a new "Companies / Organizations" group to the same watchlist file with World Labs as its first entry — the people-watch routine reads that file fresh every run and preserves its grouping, so this takes effect on the next fire with no routine-side prompt change. Broadened the watchlist preamble, CLAUDE.md's "On the daily people-watch digest" section, and the index.md entry to say the watchlist tracks organizations as well as individuals. Note: `RemoteTrigger list` from this session returned no triggers and `get` on the known trigger ids 404'd (different account scope than the one that owns the routines), so the routines could not be inspected or edited from here — verified they are still healthy instead by fetching `origin/main`, which had digests through 2026-09-02. Did not backfill `index.md`'s dated-page registrations (still stale since 2026-08-13) — separate housekeeping task, not requested here.
